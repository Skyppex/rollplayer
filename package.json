{
  "name": "rollplayer-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"cd packages/backend && bun run dev\" \"cd packages/frontend && bun run start\"",
    "dev:frontend": "cd packages/frontend && bun run start",
    "dev:backend": "cd packages/backend && bun run dev",
    "build": "cd packages/backend && bun run build && cd ../frontend && bun run build",
    "test": "cd packages/frontend && bun run test",
    "install:all": "bun install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}